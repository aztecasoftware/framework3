import { MenuItem } from '../models/menu-item';
export declare class MenuItemDirective implements MenuItem {
    name: string;
    icon: string;
    text: string;
    smallText: string;
    enabled: boolean;
}
