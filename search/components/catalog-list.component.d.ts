import { EventEmitter, QueryList } from '@angular/core';
import { CollectionView } from 'wijmo/wijmo';
import { FlexGrid, CellRangeEventArgs } from 'wijmo/wijmo.grid';
import { FlexGridFilter } from 'wijmo/wijmo.grid.filter';
import { Menu } from 'wijmo/wijmo.input';
import { CatalogItem } from '../../index';
import { SearchRequest } from '../models/search-request';
import { GridColumn, MenuItem, MenuItemDirective } from '../../controls/index';
export declare class CatalogListComponent {
    private _emptyFilter;
    private _catalogList;
    private _currentFilter;
    private _currentSortedColumn;
    private _currentSortOrder;
    private _pageSize;
    private _serverSice;
    currentItem: CatalogItem;
    catalogView: CollectionView;
    readonly pageCount: number;
    columns: QueryList<GridColumn>;
    filter: FlexGridFilter;
    grid: FlexGrid;
    contextMenu: Menu;
    contextMenuItems: QueryList<MenuItemDirective>;
    enabled: boolean;
    catalogList: CatalogItem[];
    showSelectColumn: boolean;
    showRowButton: boolean;
    rowButtonIcon: string;
    pageIndex: number;
    pageSize: number;
    serverSide: boolean;
    totalRows: number;
    searchRequested: EventEmitter<SearchRequest>;
    contextMenuClick: EventEmitter<MenuItem>;
    currentItemChanged: EventEmitter<CatalogItem>;
    itemSelected: EventEmitter<CatalogItem>;
    contextMenuOpen: EventEmitter<CatalogItem>;
    rowButtonClick: EventEmitter<any>;
    private setFilterType();
    ngAfterViewInit(): void;
    initialized(sender: FlexGrid, args: any): void;
    moveFirst(): void;
    movePrevious(): void;
    moveNext(): void;
    moveLast(): void;
    filterChanged(e: any, args: any): void;
    sortChanged(sender: FlexGrid, args: CellRangeEventArgs): void;
    createSearchRequest(pageIndex: number): SearchRequest;
    menuItemClicked(menu: Menu, args: any): void;
    onRowButtonClick(row: any): void;
}
